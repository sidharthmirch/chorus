// P3 builds the real feature/ticket tree + supervisor strip here.
// Placeholder so the P2 commit leaves `/fleet`'s Worktrees tab in an
// honest, non-broken state rather than nothing.
export function WorktreesView() {
    return (
        <div className="flex flex-1 items-center justify-center text-sm text-muted-foreground">
            Worktrees view — coming in P3.
        </div>
    );
}
